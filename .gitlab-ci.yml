pages:
  image: python:3.8
  script:
    - pip install poetry
    - poetry install
    - cd docs
    - poetry run pagekey-docgen .
    - rm -rf public
    - find .
    - mv docs/build/html public
  artifacts:
    paths:
      - public
